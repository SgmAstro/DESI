#!/bin/bash                                                                                                               
#SBATCH -p cosma                                                                                                     
#SBATCH -t 02:00:00 
#SBATCH -o gold_pipeline.log
#SBATCH -A durham 
#SBATCH --nodes=1                                                                                                          
#SBATCH --ntasks-per-node=1                                                                                               
#SBATCH --open-mode=append

# projects: hpcicc, durham, analyse, dp004, do010

# Submit with:  sbatch gold_pipeline                                                                                        
# to qos of [debug, regular]
#
# See status with e.g. squeue -u mjwilson                                                                                        
#

if [[ -z "${RESET}" ]]; then
  export RESET=1
else
  echo 'RESET SET TO BE '$RESET
fi

if [ -z ${DRYRUN+x} ]; then
  # export DRYRUN=''                                                                                                                                                                               
  export DRYRUN='--dryrun'
else
  echo 'DRYRUN SET TO BE '$DRYRUN
fi

if [ -z ${NOOVERWRITE+x} ]; then
  export NOOVERWRITE=''                                                                                                                                                                             
else
  echo 'NOOVERWRITE SET TO BE '$NOOVERWRITE
fi

# export CODE_ROOT=$HOME/DESI/
# export TILING_CATDIR=$HOME/data/
# export GOLD_DIR=$HOME/data/GAMA4/
# export RANDOMS_DIR=$HOME/data/GAMA4/randoms/

export PATH=$HOME/.conda/envs/lumfn/bin/:$CODE_ROOT/bin/:$PATH
export PYTHONPATH=$CODE_ROOT:$PYTHONPATH

echo
echo 'Environment:'
echo
echo 'Code: '$CODE_ROOT
echo 'Gold output dir.: '$GOLD_DIR
echo 'Randoms output dir: '$RANDOMS_DIR

cd $CODE_ROOT

# source /project/projectdirs/desi/software/desi_environment.sh master

if (( $RESET > 0 )); then
    echo '>>>>>  TRASHING GOLD_DIR  <<<<<.'

    rm $GOLD_DIR/*.fits
fi

echo 'Running gold pipeline; logging to logs/gama_gold.log'

python gama_gold.py $NOOVERWRITE > $GOLD_DIR/logs/gama_gold.log

echo 'Running gen_kEcat.py; logging to logs/gama_kE.log'

python gen_kEcat.py $DRYRUN $NOOVERWRITE > $GOLD_DIR/logs/gama_kE.log

echo 'Running gen_zmax_cat.py; logging to logs/gama_zama.log'

# Max. runtime 60 minutes.
python gen_zmax_cat.py $DRYRUN $NOOVERWRITE > $GOLD_DIR/logs/gama_zmax.log

echo 'Running ddp_limits.py; logging to logs/ddp_limits.log'

python ddp_limits.py $NOOVERWRITE > $GOLD_DIR/logs/ddp_limits.log

echo 'Running gen_ddp_cat.py; logging to logs/gama_ddp.log'

python gen_ddp_cat.py $DRYRUN $NOOVERWRITE > $GOLD_DIR/logs/gama_ddp.log  

echo 'Running gen_gold_lf.py; logging to logs/gama_lumfn.log'
echo
echo 'Done.'
